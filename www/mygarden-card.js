class MyGardenCard extends HTMLElement { setConfig(c){this.config=c||{};this.innerHTML=`<ha-card><div class="content"><h2>🌿 MyGarden</h2><div id="body">Chargement…</div></div></ha-card>`;this.refresh()} set hass(h){this._hass=h} async refresh(){try{const u=this.config.api||'/api/tasks/today';const d=await fetch(u).then(r=>r.json());this.querySelector('#body').innerHTML=d.length?d.map(x=>`<div>🌱 ${x.common_name} · ${x.kind}</div>`).join(''):'Aucune tâche aujourd’hui';}catch(e){this.querySelector('#body').textContent='MyGarden indisponible'}} getCardSize(){return 2}} customElements.define('mygarden-card',MyGardenCard);